# Implementation Baseline

The canonical implementation baseline is maintained in the companion artifact:
`math_learning_product_v0_34_implementation_baseline.md`.

This repository intentionally does not duplicate the full specification; source-of-truth product artifacts stay outside application code and are versioned independently.
